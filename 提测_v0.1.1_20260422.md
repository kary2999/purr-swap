# 提测申请 · USDT 换汇

| 字段 | 值 |
|---|---|
| 提测版本 | `v0.1.1` |
| 上一版本 | `v0.1.0` (首发) |
| 提测时间 | 2026-04-22 |
| 提测人 | admin |
| 平台 | Android 7.0+ / macOS 10.14+ (M1-M4 + Intel) |
| 产物 | `dist/USDT换汇-v0.1.1-release.zip` (55 MB) |
| SHA256 | `43dbde99b9dd8b7a5fd084f9e5b7aa390ab5310bd30d41748611fda73b2bdb7b` |

---

## 一、本次提测改动

### 1.1 变更类型

- [x] 🆕 新功能(feat):统计页月度出金/汇损对比、历史编辑删除、示例数据、离线打包
- [x] 🐛 Bug 修复(fix):保存记录 MissingPluginException、多 Chrome 窗口、CJK 字体豆腐
- [x] ⚡ 性能优化(perf):懒加载页面、Visa 端点 3 次回退+6s 超时、汇率缓存持久化
- [x] 🔧 配置/依赖(chore):切 CanvasKit + SharedPreferences web 显式注册、Go 静态二进制替代 Python
- [x] ⚠️ 破坏性变更:`ChannelMeta` 增加风险/费率字段;旧 `LocalStore` 文件数据已废弃(未发布过,无影响)

### 1.2 功能清单

#### 🆕 新功能

| 模块 | 功能 |
|---|---|
| **预测页** | 输入 USDT → 7 渠道预计到手 CNY + 风险排名 + 费用明细 |
| **记一笔** | USDT → (中转 JPY) → CNY 三段式,含段①段②偏差预览 + 月度额度追踪 |
| **历史** | **点击编辑(改时间/金额/收款人/备注)** + **左滑删除** (带确认) |
| **统计** | **月度出金柱图** / **月度汇损对比** / CNY-per-USDT 归一曲线 / 渠道排名 |
| **指南** | 熊猫/Wise/JRF/中行 ATM 转账教程 + 8 张 PDF 截图 + 下载链接 + 费用构成 |
| **设置** | **加载 100 条示例数据** / 导出 JSON 到剪贴板 / 从文本导入 / 清空 |

#### 🐛 Bug 修复

| ID | 现象 | 修复 |
|---|---|---|
| BUG-001 | 保存记录报 `MissingPluginException: getAll on plugins.flutter.io/shared_preferences` | `main.dart` 显式 `registerWebPlugins()` + `debugPrint` 副作用锚点阻止 dart2js tree-shake;`LocalStore` 改用 shared_preferences 替代 `dart:io` File |
| BUG-002 | 双击 App 多次弹多个 Chrome 窗口 | launcher 双重单例: `pgrep` 进程扫描 + AppleScript `found` 才 focus;否则 fresh launch |
| BUG-003 | 点 App 图标弹的是 Google 新标签页,不是 app 窗口 | 用 `open -n -a` 强制 Chrome 开独立实例,避免跟系统 Chrome 合并 profile |
| BUG-004 | 中文字体显示豆腐方块 | 打包 `SmileySans-Oblique.ttf` (2.5 MB) 进 assets,ThemeData 设 fontFamily + fontFamilyFallback |
| BUG-005 | 统计页"成交汇率" 曲线出现 158 尖刺 | USDT→JPY (158) 和 USDT→CNY (6.8) 被画到同一 Y 轴;改为归一化 `cnyReceived/usdtAmount` |
| BUG-006 | Tab 首次切换卡 7-17 秒 | IndexedStack 同时初始化 6 页;改 lazy build,只渲染当前 tab |
| BUG-007 | Visa API 端点周末返回 400 | 原 7 天回退 × 并发 2 次 = 16 次请求阻塞 UI;改 3 天内 + 6 秒超时 |

### 1.3 涉及模块

```
lib/main.dart                       - 启动入口 (只从磁盘加载缓存,不自动拉网络)
lib/models/exchange_record.dart     - 数据模型 + 风险元数据
lib/pages/forecast_page.dart        - 预测页 (新 tab)
lib/pages/record_page.dart          - 记账页
lib/pages/history_page.dart         - 历史页 (新增编辑弹窗/左滑删除)
lib/pages/stats_page.dart           - 统计页 (全部重写)
lib/pages/guide_page.dart           - 指南页 (4 渠道 + 图片)
lib/pages/settings_page.dart        - 设置页 (新增示例数据/导入)
lib/services/rate_cache.dart        - 汇率缓存 + SharedPreferences 持久化
lib/services/{binance,okx,wise,visa,panda}_client.dart - 5 个 HTTP 客户端
lib/services/forecast.dart          - 预测计算
lib/services/quota.dart             - 额度追踪
lib/web_plugins.dart                - web 插件显式注册
lib/storage/local_store.dart        - 本地 JSON 存储 (SharedPreferences)
native/server.go                    - Go 静态本地 server (CORS 代理 + 静态)
dist/USDT 换汇.app/Contents/MacOS/launcher - 单例启动脚本
scripts/release.sh                  - 自动化发布脚本
assets/data/sample_records.json     - 100 条示例数据
assets/fonts/ChineseFont.ttf        - 得意黑 SmileySans 2.5 MB
```

---

## 二、测试重点

### 2.1 必测用例

#### 🎯 T01 · 保存一条记录 (**BUG-001 回归核心**)

- **路径**: 记一笔 → 填表 → 点"保存记录"
- 正例: 渠道"熊猫速汇(JPY)",USDT=1000,JPY=158000,CNY=6720,备注"测试"
  - **预期**: 底部弹 "已保存 · 汇损/溢价 ¥xxx.xx" toast,表单清空,切到历史页能看到
  - **严禁**: 底部弹红色 "保存失败: MissingPluginException(...)"
- 反例: USDT=0 → 弹 "请输入有效的 USDT / CNY 金额" 不保存
- 反例: 两段渠道 JPY 不填 → 弹 "两段路径需要填中转日元金额"

#### 🎯 T02 · 加载示例数据 (**BUG-001 二次验证**)

- 设置 → 点"加载示例数据" → 确认弹窗 → 点"加载"
- **预期**: toast "已加载 xxx 条示例",切到历史页有 100 条 2026-01 至 04 记录
- 去统计页: 月度出金图有 4 根蓝+绿柱 (1-4 月),汇率曲线是平滑的 6-7 范围(**非 158 尖刺**)

#### 🎯 T03 · 历史页编辑记录 (**新功能**)

- 历史 → 点任意一条
- **预期**: 弹编辑 dialog,含:
  - 日期时间行 (点击弹 date picker + time picker)
  - 收款人下拉 (本人/配偶/父/母/子女/兄弟姐妹/其他)
  - USDT / 中转 JPY (仅两段渠道显示) / 实收 CNY / 备注
- 改金额 USDT 1000 → 2000 → 保存
- **预期**: toast "已更新";历史列表该条显示 "2000 USDT → ..."
- **严禁**: 崩溃或 `MissingPluginException`

#### 🎯 T04 · 历史页左滑删除 (**新功能**)

- 历史 → 往左滑一条
- **预期**: 红色删除背景露出,松手弹确认 dialog
- 点"取消" → dialog 消失,记录不删
- 再滑一次点"删除" → toast "已删除",列表减一条

#### 🎯 T05 · 预测页渠道对比

- 预测 tab 默认 1000 USDT,确认显示 7 条:Binance/OKX/线下/Visa/熊猫/Wise/JRF/中行
- 按 "到手 CNY" 从高到低排序,第一条有 🏆 图标显示"最优"
- 点任一行展开 → 显示费用明细(几 JPY 手续费、几 ATM、× 汇率)
- 改 USDT=10000 → 数字同步更新

#### 🎯 T06 · 风险说明外链

- 预测页下方 **风险排名** 卡片
- 点"币安 OTC" 卡片里的"📎 买U卖U的刑事法律风险" → 系统浏览器打开 houqilawyer.com
- macOS: 打开默认浏览器到该 URL
- Android: 同

#### 🎯 T07 · 多 Chrome 窗口 (**BUG-002 回归**) [macOS only]

- 彻底清干净: `pkill -9 -f "Chrome --app=http://127"` + `rm -f ~/Library/Application\ Support/USDTExchange/running.lock`
- 双击 App → 等 app 窗口出现
- **再次**双击 App 图标 / Dock 里点 App 图标
- **预期**: **仍然只有 1 个 app 窗口**,新的点击把老窗口带到最前面 (焦点变化)
- **严禁**: 出现 2 个 app 窗口 / 出现 Google 新标签页 Chrome 窗口

#### 🎯 T08 · 保存数据持久化

- 记一笔 → 保存成功
- 完全关闭 App (Cmd+Q 或右键 Dock 退出)
- 重开 → 历史页应该还有之前保存的那条

### 2.2 边界条件

| 场景 | 预期 |
|---|---|
| 网络完全断开 | 预测页显示上次缓存的汇率 + 角标"汇率更新于 HH:mm:ss";点刷新按钮显示加载中,超时 6 秒内报错可忍 |
| USDT 输入小数 0.01 | 能保存,历史显示 0 USDT → ¥... (四舍五入) |
| USDT 输入 999999 | 能保存,统计月度出金柱会超出常规范围 |
| 备注 500 字 | 正常保存,历史页 subtitle 截断显示 |
| 分辨率 1366x768 Windows 下的 Mac 用户?无此场景 | - |
| Android 横屏 | 布局适配(ListView 滚动正常) |

### 2.3 回归测试建议

- [ ] T01 保存记录能 work(第一时间重点验证)
- [ ] T02 加载示例数据能 work
- [ ] T03 + T04 编辑/删除能 work
- [ ] 所有 6 个 tab 都能切换
- [ ] 点刷新按钮能拉实时汇率(预测 / 记一笔两处按钮)
- [ ] 从设置导出 JSON → 清空 → 从文本导入 → 数据恢复

---

## 三、风险评估

### 3.1 已知风险

| 风险 | 严重度 | 缓解 |
|---|---|---|
| **币安 P2P / OKX C2C 国内被墙** | 🟡 中 | App 会报错但其他源正常。文案已提醒"走代理 WiFi"。 |
| **Wise/JRF 费率是估算值** | 🟡 中 | 写在 ChannelMeta 注释里,UI 标 "mid-market"。熊猫/中行/7Bank 是 API 实时值。 |
| **macOS 首次打开 "无法验证开发者"** | 🟡 中 | ad-hoc 签名只能绕基础 Gatekeeper,仍需用户右键 → 打开或跑 `xattr` 命令。根本解要 $99/年 Apple 开发者账号。 |
| **Chrome 没装用户无法用 Mac 版** | 🔴 高 | launcher 检测不到 Chrome 会弹 notification;短期 fallback 打开 Safari 但 CORS 会挡 API。 |
| **dart2js release tree-shake 再出现 plugin 丢失** | 🔴 高 | 已加 `debugPrint` 锚点。如后续升级 Flutter 失效,可能再现 BUG-001。需要升级后手动验证保存功能。 |

### 3.2 兼容性

- [x] **数据向前兼容**:新字段 `jpyAmount` / `jpyCnyReference` 都 nullable,旧格式 JSON 能导入(测 BUG-001 修复时已回归)
- [x] **SharedPreferences key 不冲突**:`flutter.cached_quotes_v1` / `flutter.records_v1` / `flutter.snapshots_v1` 都加 `_v1` 后缀,将来升级可无缝加 v2
- [x] **Android minSdk 24 (7.0) 95% 设备覆盖**
- [x] **macOS 10.14+ 覆盖 2018 年后所有 Mac**

### 3.3 回滚方案

#### 代码回滚
无 git tag 体系,本次是从 v0.1.0 源码直接升级。如需回滚,分发时保留 v0.1.0 的 zip 即可让用户重装上一版。

#### 数据回滚
- 升级到 v0.1.1 **不会**动用户旧数据(SharedPreferences key 未变)
- v0.1.1 读 v0.1.0 存的 `flutter.records_v1` 兼容
- 万一出问题:用户可"设置 → 清空全部数据" 回到初始状态

#### 客户端热修复
不支持 OTA 热更新,任何修复需用户重装新版。因此提测**必须确保安装包零 crash**。

---

## 四、依赖配置

### 4.1 config 变更

无。所有配置都在代码 `ChannelMeta` 里硬编码,用户端无需配置。

### 4.2 运行时依赖

| 端 | 必须 | 原因 |
|---|---|---|
| Android | Android 7.0+ | minSdk |
| macOS | 10.14+ | LSMinimumSystemVersion |
| macOS | 装 Chrome / Edge / Brave / Arc 任一 | Flutter web 壳需要 Chromium 内核 |
| macOS | 无 | 不需要 Xcode / Python / JDK,Go 二进制自包含 |

### 4.3 新增的网络 API

| API | 方法 | 说明 |
|---|---|---|
| `p2p.binance.com/bapi/c2c/v2/friendly/c2c/adv/search` | POST | Binance P2P 蓝钻 + 大宗 |
| `okx.com/v3/c2c/tradingOrders/books` | GET | OKX C2C |
| `wise.com/rates/live?source=USD&target=CNY` | GET | Wise 中间价 (USD/CNY, USD/JPY, JPY/CNY 三对) |
| `usa.visa.com/cmsapi/fx/rates` | GET | Visa 官方汇率 |
| `prod.pandaremit.com/pricing/rate/all/JPY/CNY` | GET | 熊猫速汇(返回 panda/boc/7bank 三平台实时费率) |

⚠ 所有外部 API 调用通过本地 Go server `/proxy/?url=...` 中转,解决 CORS。

### 4.4 测试环境准备

QA 测试前:

- [ ] 装 Chrome (macOS)
- [ ] Android 设备开启"安装未知来源" 权限
- [ ] 代理 WiFi(墙外网络)以测试 Binance/OKX 直连
- [ ] 如果之前装过 v0.1.0,先 `rm -rf ~/Library/Application\ Support/USDTExchange/` 清数据,再装 v0.1.1 验证首次启动

---

## 五、下游影响

- [ ] 前端:N/A (本项目就是前端)
- [ ] 后端:N/A (无自有后端,只调 5 个第三方 API)
- [x] 运营/合规:**风险页外链需要法务/合规过一遍**,避免链接到下架/违规内容
- [ ] SRE:N/A (无自有服务器)
- [ ] 接收人:目前就是开发者自用 + 发几个朋友

---

## 六、联动通知

- **发版前**:无团队,自用
- **发版后**:把 `dist/USDT换汇-v0.1.1-release.zip` 通过微信/AirDrop 发给内测用户
- **观察期**:发版后 **48 小时**密切关注崩溃反馈(无自动上报,靠用户手动说)

---

## 七、附录

### 7.1 主要改动文件清单

```
新增:
  A  assets/data/sample_records.json         # 100 条示例
  A  assets/fonts/ChineseFont.ttf            # 得意黑 2.5MB
  A  native/server.go                        # Go 本地 server
  A  scripts/release.sh                      # 自动化发布
  A  lib/web_plugins.dart                    # web 插件显式注册
  A  lib/web_plugins_stub.dart               # native stub

修改:
  M  lib/main.dart                           # registerWebPlugins + loadFromDisk
  M  lib/models/exchange_record.dart         # 费率 + 风险字段
  M  lib/services/rate_cache.dart            # 持久化 + 节流
  M  lib/services/visa_client.dart           # 3 天回退 + 超时
  M  lib/pages/stats_page.dart               # 全部重写
  M  lib/pages/history_page.dart             # 编辑/删除
  M  lib/pages/settings_page.dart            # 示例/导入
  M  lib/storage/local_store.dart            # dart:io → SharedPreferences
  M  pubspec.yaml                            # version 0.1.0 → 0.1.1
  M  dist/USDT 换汇.app/Contents/MacOS/launcher  # 单例强化
```

### 7.2 构建命令参考

```bash
# 一键重 build + 打包
./scripts/release.sh            # 保持当前版本重打
./scripts/release.sh patch      # 0.1.1 → 0.1.2

# 产物
dist/USDT 换汇-v0.1.1-macOS.dmg           # 26 MB
dist/USDT换汇-v0.1.1-universal.apk        # 58 MB
dist/USDT换汇-v0.1.1-release.zip          # 55 MB (DMG+APK+README)
```

### 7.3 验证产物完整性

```bash
shasum -a 256 -c dist/USDT换汇-v0.1.1-release.zip.sha256
# 预期: USDT换汇-v0.1.1-release.zip: OK
```

---

_提测模板 v0.0.1 · 生成于 2026-04-22_
